# Modelo de dados

Documento consolidado das entidades, campos, relacionamentos e constraints.
Proprietária: `dah-documentacao` (P-03).

## Entidades

| Entidade | Responsabilidade | Histórias |
|---|---|---|
| — | — | — |

O projeto usa o usuário padrão do Django (`django.contrib.auth.User`). Se
precisar de campos próprios no usuário, isso é decisão a registrar **antes** da
primeira migration: trocar depois exige migração manual de dados.
